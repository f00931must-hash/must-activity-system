import { db } from "../../shared/js/firebase-app.js";
import { collection, query, orderBy, onSnapshot } from "https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js";
const $=id=>document.getElementById(id);let activities=[];onSnapshot(query(collection(db,"activities"),orderBy("date","desc")),s=>{activities=s.docs.map(d=>({id:d.id,...d.data()})).filter(a=>a.published!==false);render()});$("searchInput").addEventListener("input",render);$("statusFilter").addEventListener("change",render);function render(){const kw=$("searchInput").value.trim(),st=$("statusFilter").value;const data=activities.filter(a=>(!kw||(a.title||"").includes(kw)||(a.description||"").includes(kw))&&(!st||a.status===st));$("activityList").innerHTML=data.length?data.map(card).join(""):'<div class="empty">目前沒有符合的活動</div>'}function card(a){const cap=Number(a.capacity||0),reg=Number(a.registeredCount||0),full=cap>0&&reg>=cap;return `<article class="activity-card"><span class="badge">${statusText(a.status)}${full?"｜已額滿":""}</span><h2>${esc(a.title)}</h2><div class="meta"><div>📅 ${esc(a.date||"")} ${esc(a.time||"")}</div><div>📍 ${esc(a.location||"")}</div>${cap?`<div>👥 ${reg}/${cap}</div>`:""}</div><p>${esc(a.description||"").slice(0,90)}</p><div class="card-actions"><a class="primary-btn" href="activity.html?id=${a.id}">我要報名</a><a class="ghost-btn" href="feedback.html?id=${a.id}">填寫回饋</a></div></article>`}function statusText(s){return {open:"報名中",feedback:"回饋中",closed:"已結束",draft:"草稿"}[s]||"活動"}function tagHtml(tags){
  if(!tags || !tags.length) return "";
  return `<div class="tag-row">${tags.map(t=>`<span class="tag">${esc(t)}</span>`).join("")}</div>`;
}

function esc(str){return String(str||"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
